//captura de caracter ingresado

// escribir letra elegida mostrando que ya fue usada
/*
function eliminarLetra(letras){
    for (var pos = 0; i < letras.length; pos++){
	if (letraValida == letras[pos]){
	    letras.splice(pos, 1);
	}
    }
}



 
    letras.forEach(function(letra){
	console.log("cada letra " + letra);
	if (letraValida == letra){
	    console.log("Esta letra esta siendo dibujada en el canvas");

	}else{
	    console.log('Esta letra no esta, se agrega una parte al ahorcado')
	}

    });





var campoFiltro = document.querySelector("#filtrar-tabla");

campoFiltro.addEventListener("input", function(){
    console.log(this.value);

    var pacientes = document.querySelectorAll(".paciente");
    
    if (this.value.length > 0){
        for (var i = 0; i < pacientes.length; i++){
            var paciente= pacientes[i];
            var tdNombre = paciente.querySelector(".info-nombre");
            var nombre = tdNombre.textContent;
	    var expresion = new RegExp(this.value, "i"); // "i" significa case insensitive

    	    if(!expresion.test(nombre)){
    	        paciente.classList.add("invisible");
    	    }else{
    	        paciente.classList.remove("invisible");
    	    }
        }
    }else{
        for (var i = 0; i < pacientes.length; i++){
            var paciente= pacientes[i];
	    paciente.classList.remove("invisible");
	}
	
    }
});



//console.log(campoFiltro.value); "campoFiltro" es el mismo objeto que "this"
//
*/
